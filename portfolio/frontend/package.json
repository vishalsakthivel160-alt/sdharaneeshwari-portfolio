{
  "name": "portfolio-frontend",
  "version": "1.0.0",
  "description": "Frontend for S. Dharaneeshwari's portfolio website",
  "private": true,
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "react-scripts": "5.0.1"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "node -e \"const { execSync } = require('child_process'); process.env.REACT_APP_VITE_API_URL = process.env.VITE_API_URL || process.env.REACT_APP_VITE_API_URL || process.env.REACT_APP_BACKEND_URL || ''; execSync('react-scripts build', { stdio: 'inherit', env: process.env });\"",
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  },
  "eslintConfig": {
    "extends": ["react-app"]
  },
  "browserslist": {
    "production": [">0.2%", "not dead", "not op_mini all"],
    "development": ["last 1 chrome version", "last 1 firefox version", "last 1 safari version"]
  }
}
