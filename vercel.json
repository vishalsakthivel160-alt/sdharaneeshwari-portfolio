{
  "buildCommand": "cd portfolio/frontend && npm install && npm run build",
  "outputDirectory": "portfolio/frontend/build",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
